============
Installation
============

At the command line::

    $ easy_install aldryn-apphooks-config

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv aldryn-apphooks-config
    $ pip install aldryn-apphooks-config